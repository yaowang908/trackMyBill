import React from 'react';
import { Box, Typography } from '@mui/material';

const Sum = () => {
  return (
    <Box sx={{ margin: '1em 0' }}>
      <Typography variant='h5' align='center'>
        $1,200 per month
      </Typography>
    </Box>
  );
};

export default Sum;
